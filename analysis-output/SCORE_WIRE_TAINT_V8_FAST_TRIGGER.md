Trigger the fast targeted trace of the exact labelled HOME_SCORE/AWAY_SCORE serializer, its composite result builder, helper function, direct callers, and reverse ancestry into the P2P/crypto family.

Retriggered after v7 confirmed the concrete score byte loads so the bounded v8 trace can finish before the slower full-program pass.
