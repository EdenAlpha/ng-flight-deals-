# Sparse-intervention GCA scope

This gate changes the transmitted object rather than re-coding the incumbent AR32 lattice address. The decoder free-runs the shared AR32 predictor where legal and receives exact interventions only where necessary. All reported codec sizes include the serialized model, framing, compressed event mask, and compressed correction values. Every winning stream must independently replay the full 128x30000 hard region and satisfy the unchanged survey-global hard-error tolerance.
