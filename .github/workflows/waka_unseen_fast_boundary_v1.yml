name: Waka unseen fast adaptation plus boundary v1
on:
  pull_request:
    branches: [migrated-volume-geometry-engine-v1]
    paths:
      - 'research/waka_unseen_fast_boundary_v1.py'
      - 'research/waka_unseen_topology_boundary_v1.py'
      - '.github/workflows/waka_unseen_fast_boundary_v1.yml'
  workflow_dispatch:
env:
  BENCHMARK_DEFINITION_SHA: 9b304e262182caa29a2c93e7e74e09ccb5715e92
jobs:
  waka-fast-boundary:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 240
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 1}
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - name: Install dependencies
        run: |
          pip install --quiet numpy boto3 pysz zstandard brotli
          pip install --quiet torch --index-url https://download.pytorch.org/whl/cpu
      - name: Materialize frozen benchmark manifest
        run: |
          git fetch --no-tags --depth=1 origin "$BENCHMARK_DEFINITION_SHA"
          git show "$BENCHMARK_DEFINITION_SHA:benchmarks/general_seismic_v1.json" > frozen_manifest.json
      - name: Run combined unseen-Waka fast-adapt plus boundary gate
        env: {PYTHONPATH: research}
        run: |
          python research/waka_unseen_fast_boundary_v1.py \
            --manifest frozen_manifest.json \
            --eps benchmarks/migrated_volume_global_eps_v1.json \
            --out waka_unseen_fast_boundary_latest.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: waka-unseen-fast-boundary-v1
          path: waka_unseen_fast_boundary_latest.json
          if-no-files-found: warn
          retention-days: 30
