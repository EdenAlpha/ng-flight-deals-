name: Imperial long-range nonlinear hunt
on:
  pull_request:
    branches: [imperial-information-floor-audit]
permissions: {contents: read}
jobs:
  hunt:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: pip install --quiet numpy scipy h5py zstandard boto3
      - name: Download and verify canonical Imperial record
        run: |
          python - <<'PY'
          import boto3,os,hashlib
          from botocore import UNSIGNED
          from botocore.config import Config
          s=boto3.client('s3',config=Config(signature_version=UNSIGNED))
          s.download_file('gdr-data-lake','imperialvalleydas/v1.0.0/DF__UTC_20201113_235932.602.h5','imperial.h5')
          assert os.path.getsize('imperial.h5')==415030456
          assert hashlib.md5(open('imperial.h5','rb').read()).hexdigest()=='8cb7ea8466ab48880f876fc43cb5ce75'
          PY
      - name: Hunt long-range and nonlinear structure
        run: python research/imperial_longrange_nonlinear_hunt.py imperial.h5
      - name: Surface strongest nonlocal diagnostic
        if: success()
        run: |
          python - <<'PY'
          import json
          d=json.load(open('imperial_longrange_nonlinear_hunt.json')); b=d['summary']['best']
          print(f"::notice title=Nonlocal Imperial structure hunt::{b['name']} H0={b['residual_h0_bps']:.6f} rough={b['h0_plus_overhead_bps']:.6f} derived={b['h0_minus_log2_267_bps']:.6f}")
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: imperial-longrange-nonlinear-hunt
          path: imperial_longrange_nonlinear_hunt.json
          if-no-files-found: ignore
          retention-days: 7
