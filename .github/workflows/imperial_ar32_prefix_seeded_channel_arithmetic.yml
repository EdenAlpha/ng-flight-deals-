name: Imperial AR32 prefix seeded channel arithmetic
on:
  pull_request:
    branches: [imperial-huber-ar32-coldstart-arithmetic-regions]
permissions: {contents: read}
jobs:
  gate:
    runs-on: ubuntu-24.04
    timeout-minutes: 55
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: pip install --quiet numpy h5py zstandard pysz boto3
      - name: Download canonical Imperial Valley record
        run: python -c "import boto3;from botocore import UNSIGNED;from botocore.config import Config;boto3.client('s3',config=Config(signature_version=UNSIGNED)).download_file('gdr-data-lake','imperialvalleydas/v1.0.0/DF__UTC_20201113_235932.602.h5','imperial.h5')"
      - name: Run prefix-seeded per-channel arithmetic
        run: python research/imperial_ar32_prefix_seeded_channel_arithmetic.py imperial.h5
      - name: Surface compact result
        run: |
          python - <<'PY'
          import json
          d=json.load(open('imperial_ar32_prefix_seeded_channel_arithmetic.json'))
          for r in d['rows']:
            b=r['best']
            print(f"::notice title=seeded-channel {r['region']}::mode={b['mode']}, bps={b['bps']:.5f}, baseline={r['baseline_bps']:.5f}, SZ3={r['sz3_bps']:.5f}, gainAR={b['gain_vs_baseline']:.6f}, gainSZ3={b['gain_vs_sz3']:.6f}")
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: imperial-ar32-prefix-seeded-channel-arithmetic
          path: imperial_ar32_prefix_seeded_channel_arithmetic.json
          retention-days: 7
