name: Imperial AR32 decoder NLMS

on:
  pull_request:
    paths:
      - 'research/imperial_ar32_decoder_nlms.py'
      - '.github/workflows/imperial_ar32_decoder_nlms.yml'
  workflow_dispatch:

jobs:
  gate:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install --quiet numpy numba h5py zstandard pysz boto3
      - name: Download canonical Imperial Valley record
        run: |
          python - <<'PY'
          import boto3,os
          from botocore import UNSIGNED
          from botocore.config import Config
          s=boto3.client('s3',config=Config(signature_version=UNSIGNED))
          s.download_file('gdr-data-lake','imperialvalleydas/v1.0.0/DF__UTC_20201113_235932.602.h5','imperial.h5')
          assert os.path.getsize('imperial.h5')==415030456
          PY
      - name: Run decoder synchronized NLMS gate
        run: python research/imperial_ar32_decoder_nlms.py imperial.h5
      - name: Surface compact result
        run: |
          python - <<'PY'
          import json
          d=json.load(open('imperial_ar32_decoder_nlms.json'))
          for r in d['rows']:
              b=r['best'];level='notice' if b['gain_vs_baseline']>1 else 'warning'
              print(f"::{level} title=decoder-NLMS {r['region']}::mu={b['mu']}, bps={b['bps']:.5f}, baseline={r['baseline_bps']:.5f}, staticTail={r['static_normalized_tail']['bps']:.5f}, gainAR={b['gain_vs_baseline']:.6f}, gainStatic={b['gain_vs_static_tail']:.6f}, zero={b['zero_fraction_tail']:.5f}")
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: imperial-ar32-decoder-nlms
          path: imperial_ar32_decoder_nlms.json
          retention-days: 7
