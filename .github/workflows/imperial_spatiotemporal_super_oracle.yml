name: Imperial spatiotemporal super oracle

on:
  pull_request:
    paths:
      - 'research/imperial_spatiotemporal_super_oracle.py'
      - '.github/workflows/imperial_spatiotemporal_super_oracle.yml'
  workflow_dispatch:

jobs:
  gate:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install --quiet numpy h5py zstandard pysz boto3
      - name: Download canonical Imperial Valley record
        run: |
          python - <<'PY'
          import boto3,os
          from botocore import UNSIGNED
          from botocore.config import Config
          s=boto3.client('s3',config=Config(signature_version=UNSIGNED))
          s.download_file('gdr-data-lake','imperialvalleydas/v1.0.0/DF__UTC_20201113_235932.602.h5','imperial.h5')
          assert os.path.getsize('imperial.h5')==415030456
          PY
      - name: Run impossible combined oracle
        run: python research/imperial_spatiotemporal_super_oracle.py imperial.h5
      - name: Surface compact result
        run: |
          python - <<'PY'
          import json
          d=json.load(open('imperial_spatiotemporal_super_oracle.json'))
          for r in d['rows']:
              o=r['spatiotemporal_super_oracle'];t=r['temporal_oracle']
              print(f"::notice title=super-oracle {r['region']}::super={o['bps']:.5f} bps, temporal={t['bps']:.5f}, baseline={r['baseline']['bps']:.5f}, SZ3={r['sz3']['bps']:.5f}, gainTemporal={o['gain_vs_temporal_oracle']:.6f}, ratio2x={o['ratio_to_2x_target']:.6f}, zero={o['zero_fraction']:.5f}")
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: imperial-spatiotemporal-super-oracle
          path: imperial_spatiotemporal_super_oracle.json
          retention-days: 7
